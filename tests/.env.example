# Tier 1 以降でホスト実行のシリアルポートを指定するときに使う。
# 名前は sketch.yaml のプロファイル名を大文字にしたもの（pytest-embedded の規約）。
TEST_SERIAL_PORT_HOST=socket://localhost

# HTML レポートを出すとき
#PYTEST_ADDOPTS="--html=report.html --self-contained-html"
