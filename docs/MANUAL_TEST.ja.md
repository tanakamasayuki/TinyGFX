# 手動テスト（実機）

内部の記録。日本語のみ。**自動テストには含めない。**

ホストのテストで守れないのは次の 3 つ。いずれも**実機でしか分からない**。

1. パネルの初期化列と待ち時間が足りているか
2. SPI のモード・ビット順・クロックが合っているか
3. **MADCTL の値と原点オフセットの導出が正しいか**（`tests/window/` は
   「実装が表どおりか」しか見ていない。表そのものが正しいかは実機で確かめる）

## M0 — M5Stack Core / BASIC で 1 画面出す（**最初にこれ**）

| | |
| --- | --- |
| 機材 | M5Stack Core / BASIC（内蔵 ILI9342C 320x240）。**配線は無し** |
| スケッチ | `examples/M5StackBasic` |
| バス | `TinyGFXBusSPI`（SCK 18 / MOSI 23 は Arduino Core の既定） |
| 焼き方 | `arduino-cli compile --profile m5stack -u -p /dev/ttyUSB0 examples/M5StackBasic` |

M1（CH32V003 + ST7789 モジュール）を先にやると、**配線・電源・コア・
ライブラリのどれが悪いのか切り分けられない**。M5Stack は配線が無く、ピンが
決まっていて、電源が安定していて、フラッシュにも余裕がある。まず「ライブラリは
動く」を確定させてから、条件の厳しい方へ降りる。

出るはずのもの（1 画面に全部入れてある）:

- [x] 外周 1 ドットの白い枠が**四辺とも**見える（大きさとオフセット）
- [x] 上の帯が**左から 赤・緑・青**（色順 / BGR ビット）
- [x] まん中の `0123456789` が鏡像でも上下逆でもない（MADCTL の向き）
- [x] 下の円・三角・線が欠けずに出る（プリミティブ）
- [x] 背景が黒（白地なら反転が逆）— **世代による**。下記

**2026-08-28、M5Stack BASIC で正常に描画。** 既定値（`INVON` / BGR / ミラー無し /
回転 0 = 320x240）がそのまま正しかった。当てずっぽうだった MADCTL の表は当たっていた。

違ったときの直し方。**どれもスケッチ 1 行**:

| 症状 | 直す場所 |
| --- | --- |
| 真っ暗 | バックライト GPIO32 が HIGH か。ここが 9 割 |
| 白地に黒 | `panel.invertDisplay(false)`。**`lcd.begin()` の後で**。古い世代の BASIC がこれ |
| 帯が 青・緑・赤 | `panel.setRgbOrder(false)` |
| 数字が鏡像 / 逆さま | `panel.setMirror(mx, my)` を `lcd.begin()` より前に |
| 端が欠ける・ずれる | コンストラクタが 320x240 になっているか |

**MADCTL のベース値・色順・回転 0 の向きは確認できた**（2026-08-28）。既定のまま
正しいので `setRotation()` は変更しない。`tests/ili9342/` の期待値も現状のままでよい。

**反転だけは世代で違う。** 手元の BASIC は `INVON`（既定）で正しく、
**古い世代の BASIC は色が反転した**。パネルクラスの既定は変えず、
スケッチ側で `invertDisplay(false)` を外す形にした
（`examples/M5StackBasic` にコメントアウトで置いてある）。

`invertDisplay()` / `setSleep()` / `displayOn()` は **`lcd.begin()` の後に呼ぶこと。**
前に呼ぶと (1) バスがまだ初期化されておらず、(2) 通っても `init()` の初期化列に
上書きされる。**2 つ重なるので「効かない」ように見える。**

### M0b — 残りを一気に確かめる（M0 が出てから）

| | |
| --- | --- |
| 機材 | M0 と同じ。**配線なし** |
| スケッチ | `tests/manual/m5stack` |
| 焼き方 | `arduino-cli compile --profile m5stack -u -p /dev/ttyUSB0 tests/manual/m5stack` |

**ホストのテストで原理的に守れないものだけ**を 1 本に詰めてある。
ページは 4 秒で自動送り。Btn A で次、Btn C で前。何を見ればいいかは
画面のページ番号（左上）とシリアル（115200）の両方に出る。

#### ページ 0-3 — 回転 0..3 【**いちばん見たい**】

回転 0 は M0 で確認済みだが、**1..3 は表が実機で正しいか分からない**。
`tests/window/` も `tests/ili9342/` も「実装が表どおりか」しか見ていない。

- [ ] 4 ページで絵が 90 度ずつ回る（飛ばない・鏡像にならない）
- [ ] 数字がどの回転でも**読める向き**で出る
- [ ] 左上の赤い四角が、回るたびに**画面の左上に来る**（論理原点）
- [ ] 枠が四辺とも見える（どの回転でも端が欠けない）
- [ ] シアンの数字が回転 0/2 で `320:240`、**回転 1/3 で `240:320`**

ずれたら `TinyGFXPanelILI9342::setRotation()` の表を直し、
`tests/ili9342/` の期待値も一緒に直す。

#### ページ 4 — 文字 【**2 番目に見たい**】

> 連鎖のベースラインを見るページは外した（2026-08-28）。**形式をまたぐ連鎖を
> やめた**ので、手で組まないと再現できなくなったため。同じ性質は
> `tests/fontchain/` がホストで検査している。

**CellFont に移してから 1 度も実機に出していない。** 内部の座標系が
「行の上端」から「ベースライン」に変わったので、ここは実物で見ておきたい。

- [ ] 倍角 1..4 で字が崩れない（1 倍の形がそのまま拡大される）
- [ ] **緑の背景セルが字の下に隙間なく敷かれる**（字だけ浮いていない・ずれていない）
- [ ] 背景ありの `0123` の次の `45` に緑が残らない（透過に戻る）
- [ ] `01ABC23` の `ABC` が**空白になる**（このフォントに英字は無い。豆腐も無い）

#### ページ 5 — クリップ

- [ ] 灰色の枠の**外側に 1 画素も出ていない**（円・線・文字とも）
- [ ] 枠の内側は欠けていない

#### ページ 6 / 7 — ちらつき

同じ絵・同じ動きを、6 が直接描画、7 が帯レンダリング（`TinyGFXTileCanvas`）で出す。

- [ ] **6 はちらつく**（消してから描くので、消えた瞬間が見える）
- [ ] **7 はちらつかない**
- [ ] 6 と 7 で**絵が同じ**（帯にしても位置も色も変わらない）

#### ページ 8 — 速度

シリアルに ms が出る。画面にも数字だけ出る（上から順）。

```
fillScreen x5 / fillRect 100x100 x200 / drawLine diag x200 /
fillCircle r100 x200 / drawString 10 chars x200 / TileCanvas full frame x20
```

- [x] **2026-08-28 取得。**[FOOTPRINT.ja.md](FOOTPRINT.ja.md) §7 に記録した

そのあと 2 つ変えて測り直すと、効くところが分かる。どちらも 1 行:

| 変える場所 | 何が分かるか |
| --- | --- |
| `TinyGFXBusSPI bus(..., 40000000UL)` | SPI クロックが効くか（24 → 40MHz） |
| `#define TINYGFX_FILL_CHUNK 32` を `#include <TinyGFX/BusSPI.h>` の前に | まとめ書きが効くか。**絵が変わらないこと**も確認（M3 の項目） |

**1 回目の結果を見て `TINYGFX_FILL_CHUNK` の実装を直した**（効かない作りだった。
[FOOTPRINT.ja.md](FOOTPRINT.ja.md) §7.1）。**上の 2 つはまだ測っていない。**

#### ページ 9 — 読み戻し 【**結果は下の「読み戻し」を見ること**】

パネルから読み戻せれば、**描いて読み戻して比べる**で実機を自動検証できる。
ホストのテストと同じ厳しさが実機に持ち込める。まず線が繋がっているかを見る。

シリアルに生のバイトが出る。

- [ ] `RDDID` / `RDID4` が **00 でも FF でもない**（MISO が来ている）
- [ ] `RAMRD` が `F8 00 00` `00 FC 00` に近い並びを返す（赤・緑を書いた 2 画素）

**全部 00 か全部 FF なら読み出しは無理。** そのときはこの道を閉じる。
先頭のダミーバイト数と RGB666 の詰め方は、返ってきた並びを見て決める。

## M1 — CH32V003 + ST7789 で構成 B が出る

| | |
| --- | --- |
| 機材 | CH32V003 ボード、ST7789 240x240 モジュール、WCH-LinkE |
| スケッチ | `examples/HelloWorld`（文字を除いたもの）または `tests/constructs/b` |
| バス | `TinyGFXBusSoftSPI`（このコアに SPI ライブラリが無い） |

- [ ] 電源投入後、画面が黒く塗りつぶされる（`fillScreen`）
- [ ] 矩形が指定した位置・大きさ・色で出る
- [ ] 色が反転していない（ST7789 は `INVON` 前提。おかしければ `invertDisplay(false)`）
- [ ] 赤・緑・青が入れ替わっていない（MADCTL の RGB/BGR ビット）
- [ ] 端の 1 列・1 行が欠けていない、余分に出ていない（オフセットのずれ）

**失敗したときに疑う順**: オフセット → INVON/INVOFF → RGB/BGR → SPI モード → 初期化列の待ち時間。

M0 が通っていれば、ここで疑うのは**配線と電源とコア**であってライブラリではない。

## M2 — 回転 0..3 が正しい向きで出る（**MADCTL の側は自動化済み**）

### 自動で守れるようになった分（2026-08-29）

`tests/hw/m5stack/` の **`test_rotation_maps`**。M5Stack BASIC で**通っている。**

読み戻しで回転を見るには一手要る。**回転 N のまま描いて回転 N のまま読んでも
何も分からない** — RAMRD は書き込みと同じアドレスカウンタを通るので、MADCTL が
間違っていても自己無矛盾に読めてしまう。

なので**回転 N で描いてから回転 0 に戻して読む。** MADCTL はアクセスの写像を
変えるだけで GRAM の中身は動かさないので、回転 0 という固定フレームから
「物理的にどこに入ったか」が見える。

`PanelILI9342.h` の MADCTL 表からの導出（MV が転置、MX が列反転、MY が行反転）。
320x240 の横長 GRAM で:

| 回転 | MADCTL | 論理 (lx, ly) → 物理 |
| --- | --- | --- |
| 0 | 0 | (lx, ly) |
| 1 | MV\|MX | (W−1−ly, lx) |
| 2 | MX\|MY | (W−1−lx, H−1−ly) |
| 3 | MV\|MY | (ly, H−1−lx) |

論理 (5, 2) に 1 画素ずつ、回転ごとに別の色で打つと 4 つとも別の隅に落ちる。
古い画素が残っていても色が違うので偽の合格にならない。**実測 `F800 07E0 001F
FFFF`** — 4 回転とも予測どおりだった。

### 自動では守れない分（目で見るしかない）

- **回転 0 自体の向き。** 上下が逆でも読み戻しは自己無矛盾になる。M0 で一度
  確認済み。ここが固定されている前提で、上のテストが 1〜3 を縛る
- **オフセットのあるモジュール。** M5Stack はオフセットが無いので
  `setGramSize()` からの導出は試せていない

| | |
| --- | --- |
| 機材 | M1 と同じ。**オフセットのあるモジュール**（135x240 など） |
| スケッチ | 左上と右下に別色の矩形を置き、`setRotation(0..3)` を順に |

- [ ] 回転 0..3 で絵が 90 度ずつ回る（飛んだり鏡像になったりしない）
- [ ] どの回転でも端が欠けない・余分が出ない
- [ ] `width()` / `height()` が回転 1/3 で入れ替わる
- [ ] **オフセットのあるモジュールで、回転 2 と 3 がずれない**

最後の項目が本番。`setGramSize()` からの導出（[CORE_DESIGN.ja.md](CORE_DESIGN.ja.md) §5、
`tests/window/` の表）が正しいかはここでしか分からない。
**ずれたら表を直し、`tests/window/` の期待値も直す。**

## M3 — ESP32 で同じスケッチが動く

| | |
| --- | --- |
| 機材 | ESP32 ボード、同じ ST7789 |
| スケッチ | M1 と同じもの。**ピン定義だけ変える** |

- [ ] ピン定義以外を変えずに同じ絵が出る
- [ ] `TinyGFXBusSPI`（ハードウェア SPI）でも同じ絵が出る
- [ ] `TINYGFX_FILL_CHUNK` を付けても絵が変わらない（速くなるだけ）

## M4 — ちらつき

| | |
| --- | --- |
| スケッチ | `examples/FlickerFree` |

**ちらつきの有無そのものは M0b のページ 6 / 7 で先に見られる。**
ここは CH32V003（帯 1 行）で成立するかの確認。

- [ ] ボールが動いてもちらつかない
- [ ] 帯の行数を変えても**見た目が変わらない**（速さだけ変わる）
- [ ] 直接描画（`fillScreen` してから描く）と見比べて、ちらつきの差が分かる

## M5 — ILI9341 が出る（**実機未確認**）

| | |
| --- | --- |
| 機材 | ILI9341 の SPI ブレイクアウト（2.4"/2.8" のよくあるやつ） |
| スケッチ | `examples/HelloWorld` のパネルを `TinyGFXPanelILI9341` に |

共有部分（`PanelDcs`）は ILI9342C で検証済みなので、**未確認なのは 3 行だけ。**

- [ ] 絵が出る（GRAM 240x320 が合っている）
- [ ] 反転していない（ILI9341 の既定は INVOFF。逆なら `invertDisplay(true)`）
- [ ] 左右が鏡像になっていない（既定で `setMirror(true, false)` を入れてある。
      逆なら `setMirror(false, false)`）
- [ ] 赤と青が入れ替わっていない（既定 BGR。逆なら `setRgbOrder(false)`）

どれも begin() の後の 1 行で直る。**直したら既定値の方を直すこと。**

## M6 — SH1106 が出る（**実機未確認**）

| | |
| --- | --- |
| 機材 | SH1106 の 1.3" OLED（「SSD1306」として売られていることが多い） |
| スケッチ | `examples/OledI2C` のパネルを `TinyGFXPanelSH1106` に |

配線は `tests/sh1106/` がホストで検証済み — **バイト列を SH1106 の仕様どおりに
解釈すれば SSD1306 と 1 ビットも違わない絵になる**ところまでは分かっている。
本物がその解釈で動くかだけが未確認。

- [ ] 絵が出る
- [ ] **左右に 2 画素ずれていない。** ずれていたら `setColumnOffset()`
      （既定 2。132 カラム RAM に 128 カラムのガラスが中央に乗る前提）
- [ ] 端にゴミの縦縞が出ない（同上）
- [ ] 帯（`setBandPage()`）でも同じ絵になる

## 記録

やったら結果をここに追記する。**やっていないものを「たぶん大丈夫」で埋めない。**

| 日付 | 項目 | 機材 | 結果 |
| --- | --- | --- | --- |
| 2026-08-28 | **M0** | M5Stack BASIC（ILI9342C） | **通過。** 既定（`INVON` / BGR / ミラー無し）のまま正常に描画 |
| 2026-08-28 | M0（反転） | M5Stack BASIC（**古い世代**） | **色が反転。** `invertDisplay(false)` を `begin()` の後に入れて解決 |
| 2026-08-28 | M0b ページ 8（速度） | M5Stack BASIC | **取得。**[FOOTPRINT.ja.md](FOOTPRINT.ja.md) §7。塗り 2.3µs/画素、点 19.8µs/画素 |
| 2026-08-28 | M0b 全体 | M5Stack BASIC | 「動いているように見える」との報告。**個別項目（回転の向き・ベースライン・背景セル）は未確認** |
| 2026-08-28 | M0b ページ 9（読み戻し） | M5Stack BASIC | **部分的に成功。** 下記 |
| 2026-08-28 | **Tier 3 自動テスト** | M5Stack BASIC | **通過。** `tests/hw/m5stack/`。実機の絵がホストのゴールデンと 1 画素も違わない |
| — | M1 / M2 / M3 / M4 | — | 未 |

## 読み戻し（2026-08-28、M5Stack BASIC）

**動くようになった。実機の GRAM がホストのゴールデンと 1 画素も違わないことを、
毎回のテストで確かめている**（`tests/hw/m5stack/`）。

### 配線

| | |
| --- | --- |
| データ線 | **SDA は GPIO23 の 1 本きり**（MOSI と MISO の兼用） |
| SPI の MISO（GPIO19） | **何も来ていない。** どう設定しても FF しか返らない |
| 形式 | ダミー 1 バイト → 1 画素 3 バイトの RGB666（上位 6 ビット） |

### 効く条件は 4 つ。**どれ 1 つ外しても読めない**

全部実測で詰めた。順に、外したときに何が起きるかも書いておく。

| 条件 | 外すとどうなるか |
| --- | --- |
| **待ちを入れない。** 手叩きのループはタイトに | 1 エッジに `delayMicroseconds(1)` を入れるだけで**全バイト FF**。パネルが線を離す |
| **取得は `pinMode` だけ。`SPI.end()` を先に呼ばない** | 呼ぶと**全バイト FF** |
| **1 画素ごとに窓（CASET/RASET）を張り直す** | 連続読みでは**カラムが進まず、同じ画素が返り続ける**（生ダンプで `FC0000` が 5 回） |
| **2 回読んで一致するまで繰り返す** | 20 バイトに 1 回ほどビットが化ける。**クロックを落として直すことはできない**（上記のとおり止まる） |
| **戻すのは `SPI.end()` → `SPI.begin()`** | `begin()` だけだと何も起きず（開始済みなら早期 return）、**以降の書き込みが黙って死ぬ**。同じ絵を描き直すと気づけないので厄介 |

**「安全側に倒して遅くする」が通じない相手だった。** 私が良かれと思って入れた
`delayMicroseconds` が、そのまま読めない原因になっていた。

### 効かなかったもの

- `0xF6` Interface Control の `SDA_EN`（0 でも 1 でも変わらない）
- 読み線を GPIO19 にする（何も来ていない）
- ID レジスタ `0x04` / `0x09` / `0x0A` / `0x0C`（基本 FF。**GRAM は読めるのに**）

### 速さ

**1 画素あたり約 150µs。** 1 画素ごとに窓を張り直すので、コマンド 14 バイト +
読み 4 バイトを手で叩くぶん。64x8 の帯で 75ms 程度。
**デバッグと検証のためのもので、描画に使うものではない。**

### これで何が守れるようになったか

`tests/hw/m5stack/` の 4 本のうち、**読み戻しを使う 1 本だけが線の先を見ている。**

| テスト | 見ている範囲 |
| --- | --- |
| `test_scene_matches_host_golden` | 実機の CPU・コンパイラ・PROGMEM を通った描画（`BusCapture`） |
| **`test_readback_matches_golden`** | **パネルの GRAM そのもの。線・SPI・パネルの解釈まで** |
