# 手動テスト（実機）

内部の記録。日本語のみ。**自動テストには含めない。**

ホストのテストで守れないのは次の 3 つ。いずれも**実機でしか分からない**。

1. パネルの初期化列と待ち時間が足りているか
2. SPI のモード・ビット順・クロックが合っているか
3. **MADCTL の値と原点オフセットの導出が正しいか**（`tests/window/` は
   「実装が表どおりか」しか見ていない。表そのものが正しいかは実機で確かめる）

## M0 — M5Stack Core / BASIC で 1 画面出す（**最初にこれ**）

| | |
| --- | --- |
| 機材 | M5Stack Core / BASIC（内蔵 ILI9342C 320x240）。**配線は無し** |
| スケッチ | `examples/M5StackBasic` |
| バス | `TinyGFXBusSPI`（SCK 18 / MOSI 23 は Arduino Core の既定） |
| 焼き方 | `arduino-cli compile --profile m5stack -u -p /dev/ttyUSB0 examples/M5StackBasic` |

M1（CH32V003 + ST7789 モジュール）を先にやると、**配線・電源・コア・
ライブラリのどれが悪いのか切り分けられない**。M5Stack は配線が無く、ピンが
決まっていて、電源が安定していて、フラッシュにも余裕がある。まず「ライブラリは
動く」を確定させてから、条件の厳しい方へ降りる。

出るはずのもの（1 画面に全部入れてある）:

- [x] 外周 1 ドットの白い枠が**四辺とも**見える（大きさとオフセット）
- [x] 上の帯が**左から 赤・緑・青**（色順 / BGR ビット）
- [x] まん中の `0123456789` が鏡像でも上下逆でもない（MADCTL の向き）
- [x] 下の円・三角・線が欠けずに出る（プリミティブ）
- [x] 背景が黒（白地なら反転が逆）— **世代による**。下記

**2026-08-28、M5Stack BASIC で正常に描画。** 既定値（`INVON` / BGR / ミラー無し /
回転 0 = 320x240）がそのまま正しかった。当てずっぽうだった MADCTL の表は当たっていた。

違ったときの直し方。**どれもスケッチ 1 行**:

| 症状 | 直す場所 |
| --- | --- |
| 真っ暗 | バックライト GPIO32 が HIGH か。ここが 9 割 |
| 白地に黒 | `panel.invertDisplay(false)`。**`lcd.begin()` の後で**。古い世代の BASIC がこれ |
| 帯が 青・緑・赤 | `panel.setRgbOrder(false)` |
| 数字が鏡像 / 逆さま | `panel.setMirror(mx, my)` を `lcd.begin()` より前に |
| 端が欠ける・ずれる | コンストラクタが 320x240 になっているか |

**MADCTL のベース値・色順・回転 0 の向きは確認できた**（2026-08-28）。既定のまま
正しいので `setRotation()` は変更しない。`tests/ili9342/` の期待値も現状のままでよい。

**反転だけは世代で違う。** 手元の BASIC は `INVON`（既定）で正しく、
**古い世代の BASIC は色が反転した**。パネルクラスの既定は変えず、
スケッチ側で `invertDisplay(false)` を外す形にした
（`examples/M5StackBasic` にコメントアウトで置いてある）。

`invertDisplay()` / `setSleep()` / `displayOn()` は **`lcd.begin()` の後に呼ぶこと。**
前に呼ぶと (1) バスがまだ初期化されておらず、(2) 通っても `init()` の初期化列に
上書きされる。**2 つ重なるので「効かない」ように見える。**

### M0b — 残りを一気に確かめる（M0 が出てから）

| | |
| --- | --- |
| 機材 | M0 と同じ。**配線なし** |
| スケッチ | `tests/manual/m5stack` |
| 焼き方 | `arduino-cli compile --profile m5stack -u -p /dev/ttyUSB0 tests/manual/m5stack` |

**ホストのテストで原理的に守れないものだけ**を 1 本に詰めてある。
ページは 4 秒で自動送り。Btn A で次、Btn C で前。何を見ればいいかは
画面のページ番号（左上）とシリアル（115200）の両方に出る。

#### ページ 0-3 — 回転 0..3 【**いちばん見たい**】

回転 0 は M0 で確認済みだが、**1..3 は表が実機で正しいか分からない**。
`tests/window/` も `tests/ili9342/` も「実装が表どおりか」しか見ていない。

- [ ] 4 ページで絵が 90 度ずつ回る（飛ばない・鏡像にならない）
- [ ] 数字がどの回転でも**読める向き**で出る
- [ ] 左上の赤い四角が、回るたびに**画面の左上に来る**（論理原点）
- [ ] 枠が四辺とも見える（どの回転でも端が欠けない）
- [ ] シアンの数字が回転 0/2 で `320:240`、**回転 1/3 で `240:320`**

ずれたら `TinyGFXPanelILI9342::setRotation()` の表を直し、
`tests/ili9342/` の期待値も一緒に直す。

#### ページ 4 — 文字 【**2 番目に見たい**】

**CellFont に移してから 1 度も実機に出していない。** 内部の座標系が
「行の上端」から「ベースライン」に変わったので、ここは実物で見ておきたい。

- [ ] 倍角 1..4 で字が崩れない（1 倍の形がそのまま拡大される）
- [ ] **緑の背景セルが字の下に隙間なく敷かれる**（字だけ浮いていない・ずれていない）
- [ ] 背景ありの `0123` の次の `45` に緑が残らない（透過に戻る）
- [ ] `01ABC23` の `ABC` が**空白になる**（このフォントに英字は無い。豆腐も無い）

#### ページ 5 — 連鎖のベースライン

高さの違う 2 つの CellFont を繋いである。**`A` `B` `C` は 3 画素の帯。**

- [ ] `ABC` の帯が**数字の下端に揃う**（上端に出たら換算が間違っている）
- [ ] 倍角 1..4 のどれでも揃っている
- [ ] 1 行目の帯が、灰色の目安線の上に乗っている

#### ページ 6 — クリップ

- [ ] 灰色の枠の**外側に 1 画素も出ていない**（円・線・文字とも）
- [ ] 枠の内側は欠けていない

#### ページ 7 / 8 — ちらつき

同じ絵・同じ動きを、7 が直接描画、8 が帯レンダリング（`TinyGFXTileCanvas`）で出す。

- [ ] **7 はちらつく**（消してから描くので、消えた瞬間が見える）
- [ ] **8 はちらつかない**
- [ ] 7 と 8 で**絵が同じ**（帯にしても位置も色も変わらない）

#### ページ 9 — 速度 【**まだ 1 度も測っていない**】

シリアルに ms が出る。画面にも数字だけ出る（上から順）。

```
fillScreen x5 / fillRect 100x100 x200 / drawLine diag x200 /
fillCircle r100 x200 / drawString 10 chars x200 / TileCanvas full frame x20
```

- [ ] 数字を控える。**速度の基準値がまだどこにも無い**

そのあと 2 つ変えて測り直すと、効くところが分かる。どちらも 1 行:

| 変える場所 | 何が分かるか |
| --- | --- |
| `TinyGFXBusSPI bus(..., 40000000UL)` | SPI クロックが効くか（24 → 40MHz） |
| `#define TINYGFX_FILL_CHUNK 32` を `#include <TinyGFX/BusSPI.h>` の前に | まとめ書きが効くか。**絵が変わらないこと**も確認（M3 の項目） |

結果は下の「記録」に貼ってほしい。[FOOTPRINT.ja.md](FOOTPRINT.ja.md) に速度の節を作る。

## M1 — CH32V003 + ST7789 で構成 B が出る

| | |
| --- | --- |
| 機材 | CH32V003 ボード、ST7789 240x240 モジュール、WCH-LinkE |
| スケッチ | `examples/HelloWorld`（文字を除いたもの）または `tests/constructs/b` |
| バス | `TinyGFXBusSoftSPI`（このコアに SPI ライブラリが無い） |

- [ ] 電源投入後、画面が黒く塗りつぶされる（`fillScreen`）
- [ ] 矩形が指定した位置・大きさ・色で出る
- [ ] 色が反転していない（ST7789 は `INVON` 前提。おかしければ `invertDisplay(false)`）
- [ ] 赤・緑・青が入れ替わっていない（MADCTL の RGB/BGR ビット）
- [ ] 端の 1 列・1 行が欠けていない、余分に出ていない（オフセットのずれ）

**失敗したときに疑う順**: オフセット → INVON/INVOFF → RGB/BGR → SPI モード → 初期化列の待ち時間。

M0 が通っていれば、ここで疑うのは**配線と電源とコア**であってライブラリではない。

## M2 — 回転 0..3 が正しい向きで出る

| | |
| --- | --- |
| 機材 | M1 と同じ。**できればオフセットのあるモジュール**（135x240 など）も |
| スケッチ | 左上と右下に別色の矩形を置き、`setRotation(0..3)` を順に |

**M0b と重ならない部分だけがここの本番。** M0b（M5Stack / ILI9342）はオフセットの
無いパネルなので、`setGramSize()` からの導出は試せていない。**オフセットのある
ST7789 でしか分からない。**

- [ ] 回転 0..3 で絵が 90 度ずつ回る（飛んだり鏡像になったりしない）
- [ ] どの回転でも端が欠けない・余分が出ない
- [ ] `width()` / `height()` が回転 1/3 で入れ替わる
- [ ] **オフセットのあるモジュールで、回転 2 と 3 がずれない**

最後の項目が本番。`setGramSize()` からの導出（[CORE_DESIGN.ja.md](CORE_DESIGN.ja.md) §5、
`tests/window/` の表）が正しいかはここでしか分からない。
**ずれたら表を直し、`tests/window/` の期待値も直す。**

## M3 — ESP32 で同じスケッチが動く

| | |
| --- | --- |
| 機材 | ESP32 ボード、同じ ST7789 |
| スケッチ | M1 と同じもの。**ピン定義だけ変える** |

- [ ] ピン定義以外を変えずに同じ絵が出る
- [ ] `TinyGFXBusSPI`（ハードウェア SPI）でも同じ絵が出る
- [ ] `TINYGFX_FILL_CHUNK` を付けても絵が変わらない（速くなるだけ）

## M4 — ちらつき

| | |
| --- | --- |
| スケッチ | `examples/FlickerFree` |

**ちらつきの有無そのものは M0b のページ 7 / 8 で先に見られる。**
ここは CH32V003（帯 1 行）で成立するかの確認。

- [ ] ボールが動いてもちらつかない
- [ ] 帯の行数を変えても**見た目が変わらない**（速さだけ変わる）
- [ ] 直接描画（`fillScreen` してから描く）と見比べて、ちらつきの差が分かる

## 記録

やったら結果をここに追記する。**やっていないものを「たぶん大丈夫」で埋めない。**

| 日付 | 項目 | 機材 | 結果 |
| --- | --- | --- | --- |
| 2026-08-28 | **M0** | M5Stack BASIC（ILI9342C） | **通過。** 既定（`INVON` / BGR / ミラー無し）のまま正常に描画 |
| 2026-08-28 | M0（反転） | M5Stack BASIC（**古い世代**） | **色が反転。** `invertDisplay(false)` を `begin()` の後に入れて解決 |
| — | **M0b** | M5Stack | **未。スケッチは用意済み**（`tests/manual/m5stack`） |
| — | M1 / M2 / M3 / M4 | — | 未 |
